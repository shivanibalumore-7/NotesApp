<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notesapp">

    <!-- कोई special permission नहीं चाहिए क्योंकि ऐप offline SQLite use करता है -->
    <!-- अगर future में share / internet functionality add करोगे तो Internet permission डाल सकते हो -->

    <application
        android:allowBackup="true"
        android:label="Notes"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- AddEditNoteActivity -->
        <activity android:name=".AddEditNoteActivity"/>

        <!-- MainActivity -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>